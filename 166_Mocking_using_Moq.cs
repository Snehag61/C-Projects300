using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Threading.Tasks;

// 166. Mocking using Moq
// Generated as a standalone sample file.
public class Problem166
{
    public static async Task Main()
    {
        Console.WriteLine("This is a Moq starter example.");
        Console.WriteLine("Install the Moq package in a test project to run it.");
        
        // var mock = new Mock<IEmailService>();
        // mock.Setup(x => x.Send(It.IsAny<string>())).Verifiable();
        // var service = new NotificationService(mock.Object);
        // service.Notify("Hello");
        // mock.Verify();

        await Task.CompletedTask;
    }
}
